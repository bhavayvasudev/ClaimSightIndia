# Env / secrets
# .env.example is intentionally NOT listed here — it's a template with no
# real values and should be version-controlled so the required variables
# are documented in the repo.
.env
.env.local
.env.example
.env.example.local
.env*.local
*.pem

# Python
__pycache__/
*.pyc
.venv/
venv/
*.egg-info/
.pytest_cache/
.ruff_cache/

# Node
node_modules/
.next/
out/
*.tsbuildinfo

# Data / artifacts
data/uploads/
evaluation/reports/
*.log
*.db

# OS
.DS_Store
Thumbs.db

ai-service/.venv/
runs/
ai-service/uploads/
*.pt
